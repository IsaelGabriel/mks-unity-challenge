using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ShipGraphics : MonoBehaviour
{

    [SerializeField] private static ShipPart[] hull = new ShipPart[2];
    [SerializeField] private static ShipPart[] sailLarge = new ShipPart[6];
    [SerializeField] private static ShipPart[] sailSmall = new ShipPart[4];
    [SerializeField] private static ShipPart[] flag = new ShipPart[6];



    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
